apiVersion: extensions.agents.x-k8s.io/v1alpha1
kind: SandboxWarmPool
metadata:
  name: agent-sandbox-warmpool
  namespace: ${NAMESPACE}
  labels:
    app: agent-sandbox-warmpool
spec:
  replicas: 2
  sandboxTemplateRef:
    name: agent-sandbox-template
