apiVersion: v1
kind: Service
metadata:
  name: inference-gateway-svc
  namespace: ${NAMESPACE}
  labels:
    app: inference-gateway
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: inference-gateway
  type: ClusterIP
