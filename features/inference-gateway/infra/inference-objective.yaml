apiVersion: inference.networking.x-k8s.io/v1alpha2
kind: InferenceObjective
metadata:
  name: inference-gateway-objective
  namespace: ${NAMESPACE}
spec:
  inferencePoolRef:
    name: advanced-inference-pool
  priorities:
  - priority: 10
    name: default
  - priority: 20
    name: critical
